name: CI

on:
  pull_request:
  push:
    branches:
      - master

permissions:
  contents: read

jobs:
  python:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version:
          - "3.12"
          - "3.13"
          - "3.14"

    steps:
      - uses: actions/checkout@v5
      - uses: astral-sh/setup-uv@v6
        with:
          enable-cache: true
          python-version: ${{ matrix.python-version }}
      - run: uv sync --all-packages --locked
      - run: uv run ruff check .
      - run: uv run mypy
      - run: uv run pytest tests --cov

  netbox-plugin:
    runs-on: ubuntu-latest
    env:
      NETBOX_CONFIGURATION: netbox_ssot_test_configuration
      PYTHONPATH: ${{ github.workspace }}/tests
    services:
      postgres:
        image: postgres:17
        env:
          POSTGRES_DB: netbox
          POSTGRES_USER: netbox
          POSTGRES_PASSWORD: netbox
        options: >-
          --health-cmd "pg_isready -U netbox -d netbox"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432
      redis:
        image: redis:7-alpine
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 6379:6379

    steps:
      - uses: actions/checkout@v5
      - uses: actions/checkout@v5
        with:
          repository: netbox-community/netbox
          ref: v4.6.9
          path: .netbox
      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: .netbox/requirements.txt
      - name: Install NetBox and plugin dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -r .netbox/requirements.txt
          python -m pip install -e packages/contracts -e providers/netbox -e packages/plugin
      - name: Check plugin migrations
        run: python .netbox/netbox/manage.py makemigrations netbox_ssot --check --dry-run
      - name: Run plugin tests
        run: python .netbox/netbox/manage.py test netbox_ssot.tests --noinput --verbosity 2

  go:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-go@v6
        with:
          go-version: "1.26.x"
          cache-dependency-path: go.mod
      - run: test -z "$(gofmt -l agent internal providers/netbox)"
      - run: go test ./...
      - run: go vet ./...
      - run: CGO_ENABLED=0 go build -trimpath -o dist/netbox-ssot-agent ./agent/cmd/netbox-ssot-agent
